import React from 'react';

const Header: React.FC = () => {
    return (
        <header>
            <h1 className="app-title">Velo Antwerp</h1>
            <p className="app-subtitle">Find available bikes near you</p>
        </header>
    );
};

export default Header;